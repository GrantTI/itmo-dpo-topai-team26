# Необходимые знания для реализации проекта

## 1. Backend (Python + FastAPI)

### 1.1. Основы Python
```python
# Базовые знания
- Синтаксис Python (3.10+)
- Работа с типами данных (list, dict, tuple, set)
- Функции, декораторы, контекстные менеджеры
- Классы и ООП (наследование, полиморфизм)
- Обработка исключений
- Работа с файлами (open, read, write)
- Генераторы и итераторы
- Асинхронное программирование (asyncio)

# Пример необходимого кода
async def process_data(file_path: str) -> pd.DataFrame:
    """Асинхронная обработка данных"""
    try:
        df = await asyncio.to_thread(pd.read_csv, file_path)
        return df
    except Exception as e:
        raise HTTPException(status_code=400, detail=str(e))
```

### 1.2. FastAPI
```python
# Основные компоненты
- Создание приложения: FastAPI()
- Роутинг: @app.get(), @app.post(), @app.put()
- Path и Query параметры
- Request Body: Pydantic модели
- Dependency Injection
- Middleware (CORS, логирование)
- Обработка ошибок: HTTPException
- Асинхронные эндпоинты
- WebSocket (опционально)
- Swagger/OpenAPI документация

# Пример
from fastapi import FastAPI, File, UploadFile, HTTPException
from pydantic import BaseModel

app = FastAPI()

class ModelParams(BaseModel):
    model_type: str
    alpha: float = 1.0
    max_iter: int = 1000

@app.post("/api/train")
async def train_model(
    params: ModelParams,
    file: UploadFile = File(...)
):
    # Логика обучения
    pass
```

### 1.3. Pandas & NumPy
```python
# Pandas
- Чтение CSV: pd.read_csv()
- DataFrame манипуляции: 
  - df.head(), df.info(), df.describe()
  - df.dropna(), df.fillna()
  - df.groupby(), df.merge()
  - df.apply(), df.map()
  - Создание признаков
  - One-Hot Encoding: pd.get_dummies()
- Работа с пропусками и выбросами
- Сохранение: df.to_csv()

# NumPy
- Массивы: np.array()
- Математические операции
- Статистика: np.mean(), np.std(), np.corrcoef()
- Линейная алгебра

# Пример
def preprocess_data(df: pd.DataFrame, target_col: str):
    X = df.drop(columns=[target_col])
    y = df[target_col]
    
    # Стандартизация
    from sklearn.preprocessing import StandardScaler
    scaler = StandardScaler()
    X_scaled = scaler.fit_transform(X)
    
    return X_scaled, y, scaler
```

### 1.4. Scikit-learn
```python
# Модели линейной регрессии
- LinearRegression
- Ridge, RidgeCV
- Lasso, LassoCV
- ElasticNet, ElasticNetCV
- SGDRegressor

# Предобработка
- StandardScaler, MinMaxScaler
- LabelEncoder, OneHotEncoder
- PolynomialFeatures

# Метрики
- r2_score
- mean_squared_error
- mean_absolute_error
- explained_variance_score

# Кросс-валидация
- cross_val_score
- cross_val_predict
- KFold, StratifiedKFold
- learning_curve

# Pipeline
from sklearn.pipeline import Pipeline

# Пример обучения
from sklearn.linear_model import Ridge
from sklearn.model_selection import cross_val_score

model = Ridge(alpha=1.0)
model.fit(X_train, y_train)
y_pred = model.predict(X_test)

# Кросс-валидация
scores = cross_val_score(model, X, y, cv=5, scoring='r2')
```

### 1.5. Matplotlib & Seaborn
```python
# Matplotlib основы
- plt.figure(), plt.subplot()
- plt.plot(), plt.scatter(), plt.bar()
- plt.xlabel(), plt.ylabel(), plt.title()
- plt.legend(), plt.grid()
- Сохранение: plt.savefig()
- Работа с Figure, Axes

# Seaborn
- sns.regplot(), sns.residplot()
- sns.boxplot(), sns.barplot()
- sns.heatmap()
- sns.pairplot()

# Пример
import matplotlib.pyplot as plt
import seaborn as sns

def plot_actual_vs_predicted(y_true, y_pred, title="Actual vs Predicted"):
    fig, ax = plt.subplots(figsize=(10, 6))
    ax.scatter(y_true, y_pred, alpha=0.5)
    ax.plot([y_true.min(), y_true.max()], 
            [y_true.min(), y_true.max()], 'r--', lw=2)
    ax.set_xlabel("Actual Values")
    ax.set_ylabel("Predicted Values")
    ax.set_title(title)
    return fig
```

### 1.6. Дополнительные библиотеки
```python
# Логирование
import logging
logging.basicConfig(level=logging.INFO)

# Работа с JSON
import json

# HTTP клиент (для тестов)
import httpx

# Работа с архивами
import zipfile

# Обработка изображений
import io
from PIL import Image

# Переменные окружения
from dotenv import load_dotenv

# Тестирование
import pytest
from fastapi.testclient import TestClient
```

---

## 2. Frontend (Vue.js 3)

### 2.1. Основы JavaScript/TypeScript
```javascript
// ES6+ синтаксис
- const, let, var
- Стрелочные функции: () => {}
- Деструктуризация: const { name } = obj
- Spread оператор: ...array
- Async/await
- Промисы: Promise, .then(), .catch()
- Map, Filter, Reduce
- Модули: import/export

// TypeScript (опционально)
- Интерфейсы и типы
- Generics
- Enums
- Union Types
```

### 2.2. Vue.js 3 Composition API
```vue
<script setup>
// Основы Composition API
import { ref, reactive, computed, watch, onMounted } from 'vue'

// Реактивные переменные
const count = ref(0)
const state = reactive({ name: 'John', age: 30 })

// Вычисляемые свойства
const doubled = computed(() => count.value * 2)

// Наблюдатели
watch(count, (newVal, oldVal) => {
  console.log(`Count changed from ${oldVal} to ${newVal}`)
})

// Жизненный цикл
onMounted(() => {
  console.log('Component mounted')
})

// Методы
const increment = () => {
  count.value++
}
</script>
```

### 2.3. Vue Router
```javascript
// Настройка маршрутизации
import { createRouter, createWebHistory } from 'vue-router'

const routes = [
  { path: '/', component: Home },
  { path: '/data', component: DataView },
  { path: '/model', component: ModelView },
  { path: '/results', component: ResultsView }
]

const router = createRouter({
  history: createWebHistory(),
  routes
})

// Использование в компонентах
import { useRouter, useRoute } from 'vue-router'
const router = useRouter()
const route = useRoute()

// Навигация
router.push('/data')
router.push({ name: 'model', params: { id: 1 } })
```

### 2.4. Pinia (State Management)
```javascript
// store/dataStore.js
import { defineStore } from 'pinia'

export const useDataStore = defineStore('data', {
  state: () => ({
    dataset: null,
    preview: [],
    targetColumn: null,
    columns: []
  }),
  
  getters: {
    hasData: (state) => state.dataset !== null,
    columnCount: (state) => state.columns.length
  },
  
  actions: {
    async uploadData(file) {
      const formData = new FormData()
      formData.append('file', file)
      
      const response = await api.uploadData(formData)
      this.dataset = response.data
      this.preview = response.preview
      this.columns = response.columns
    },
    
    setTargetColumn(column) {
      this.targetColumn = column
    }
  }
})

// Использование в компонентах
import { useDataStore } from '@/store/dataStore'
const dataStore = useDataStore()
const { hasData, columnCount } = storeToRefs(dataStore)
```

### 2.5. HTTP клиент (Axios)
```javascript
// services/api.js
import axios from 'axios'

const API_BASE_URL = import.meta.env.VITE_API_URL || '/api'

const api = axios.create({
  baseURL: API_BASE_URL,
  timeout: 30000,
  headers: {
    'Content-Type': 'application/json'
  }
})

// Интерсепторы
api.interceptors.request.use(
  config => {
    // Добавление токена
    const token = localStorage.getItem('token')
    if (token) {
      config.headers.Authorization = `Bearer ${token}`
    }
    return config
  },
  error => Promise.reject(error)
)

api.interceptors.response.use(
  response => response,
  error => {
    if (error.response.status === 401) {
      // Перенаправление на логин
    }
    return Promise.reject(error)
  }
)

// API методы
export const dataApi = {
  upload: (formData) => api.post('/data/upload', formData, {
    headers: { 'Content-Type': 'multipart/form-data' }
  }),
  
  preview: () => api.get('/data/preview')
}

export const modelApi = {
  train: (params) => api.post('/train', params),
  getMetrics: () => api.get('/train/metrics')
}
```

### 2.6. UI компоненты и библиотеки
```vue
<!-- Пример использования PrimeVue -->
<template>
  <div class="p-grid">
    <div class="p-col-12">
      <Card>
        <template #title>
          Загрузка данных
        </template>
        <template #content>
          <FileUpload 
            mode="basic"
            accept=".csv"
            :maxFileSize="10485760"
            @uploader="handleUpload"
          />
        </template>
      </Card>
    </div>
  </div>
</template>
```

### 2.7. Визуализация (Chart.js / ECharts)
```javascript
// С использованием Chart.js
import { Chart, registerables } from 'chart.js'
Chart.register(...registerables)

// Создание графика
const chart = new Chart(ctx, {
  type: 'scatter',
  data: {
    datasets: [{
      label: 'Actual vs Predicted',
      data: points,
      backgroundColor: 'rgba(75, 192, 192, 0.2)',
      borderColor: 'rgba(75, 192, 192, 1)',
      borderWidth: 1
    }]
  },
  options: {
    responsive: true,
    plugins: {
      legend: { position: 'top' },
      title: { display: true, text: 'Model Performance' }
    }
  }
})

// Vue компонент для Chart.js
<script setup>
import { ref, onMounted, watch } from 'vue'
import { Line } from 'vue-chartjs'

const props = defineProps(['data', 'options'])

onMounted(() => {
  // Инициализация
})
</script>
```

---

## 3. Docker & DevOps

### 3.1. Docker
```dockerfile
# Dockerfile для backend
FROM python:3.10-slim

WORKDIR /app

COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

COPY . .

EXPOSE 8000

CMD ["uvicorn", "app.main:app", "--host", "0.0.0.0", "--port", "8000"]

# Основные команды
docker build -t ml-backend .
docker run -d -p 8000:8000 ml-backend
docker ps
docker logs <container_id>
docker exec -it <container_id> /bin/bash
```

### 3.2. Docker Compose
```yaml
# docker-compose.yml
services:
  backend:
    build: ./backend
    ports:
      - "8000:8000"
    volumes:
      - ./data:/app/data
    environment:
      - ENVIRONMENT=development
  
  frontend:
    build: ./frontend
    ports:
      - "80:80"
    depends_on:
      - backend
    environment:
      - VITE_API_URL=http://backend:8000

# Основные команды
docker-compose up -d
docker-compose down
docker-compose logs -f
docker-compose exec backend bash
docker-compose build --no-cache
```

### 3.3. Основы Linux
```bash
# Навигация
cd /path/to/dir
ls -la
pwd

# Работа с файлами
cp source destination
mv old new
rm file.txt
mkdir new_dir
chmod +x script.sh

# Система
ps aux | grep python
kill -9 PID
df -h
free -m

# Сеть
curl http://localhost:8000
netstat -tulpn
ping google.com
```

---

## 4. Работа с Git

### 4.1. Основные команды
```bash
# Инициализация и клонирование
git init
git clone https://github.com/user/repo.git

# Работа с изменениями
git status
git add .
git commit -m "Message"
git push origin main
git pull origin main

# Ветки
git branch feature-branch
git checkout feature-branch
git merge feature-branch
git branch -d feature-branch

# Просмотр истории
git log --oneline
git diff
git show <commit-hash>
```

### 4.2. .gitignore
```gitignore
# Python
__pycache__/
*.py[cod]
*.so
.Python
env/
venv/
.venv/
.env

# Node
node_modules/
dist/
.vite/

# IDE
.vscode/
.idea/

# Docker
*.log
data/
uploads/
exports/
```

---

## 5. Дополнительные навыки

### 5.1. Тестирование
```python
# Backend тесты (pytest)
import pytest
from fastapi.testclient import TestClient
from app.main import app

client = TestClient(app)

def test_upload_file():
    with open('test.csv', 'rb') as f:
        response = client.post(
            '/api/data/upload',
            files={'file': ('test.csv', f, 'text/csv')}
        )
    assert response.status_code == 200
    assert 'preview' in response.json()

# Frontend тесты (Vitest)
import { describe, it, expect } from 'vitest'
import { mount } from '@vue/test-utils'
import FileUpload from '@/components/FileUpload.vue'

describe('FileUpload', () => {
  it('renders properly', () => {
    const wrapper = mount(FileUpload)
    expect(wrapper.text()).toContain('Upload')
  })
})
```

### 5.2. Логирование
```python
# Настройка логирования
import logging
import sys

logging.basicConfig(
    level=logging.INFO,
    format='%(asctime)s - %(name)s - %(levelname)s - %(message)s',
    handlers=[
        logging.FileHandler('app.log'),
        logging.StreamHandler(sys.stdout)
    ]
)

logger = logging.getLogger(__name__)

# Использование
logger.info(f"Model trained with R2={r2_score:.4f}")
logger.error(f"Failed to load data: {str(e)}")
```

### 5.3. Обработка ошибок
```python
# Кастомные исключения
class DataValidationError(Exception):
    pass

class ModelTrainingError(Exception):
    pass

# Обработка в FastAPI
@app.exception_handler(DataValidationError)
async def data_validation_handler(request, exc):
    return JSONResponse(
        status_code=400,
        content={"error": str(exc)}
    )
```

### 5.4. WebSocket (опционально)
```python
# Backend WebSocket
from fastapi import WebSocket

@app.websocket("/ws/training")
async def websocket_endpoint(websocket: WebSocket):
    await websocket.accept()
    try:
        while True:
            data = await websocket.receive_text()
            # Отправка прогресса обучения
            await websocket.send_json({"progress": 50})
    except WebSocketDisconnect:
        print("Client disconnected")
```

```javascript
// Frontend WebSocket
const ws = new WebSocket('ws://localhost:8000/ws/training')

ws.onopen = () => {
  console.log('WebSocket connected')
}

ws.onmessage = (event) => {
  const data = JSON.parse(event.data)
  if (data.progress) {
    progress.value = data.progress
  }
}

ws.onerror = (error) => {
  console.error('WebSocket error:', error)
}
```

---

## 6. Рекомендуемые ресурсы для изучения

### 6.1. Python & FastAPI
- **Python**: [Official Tutorial](https://docs.python.org/3/tutorial/)
- **FastAPI**: [Official Docs](https://fastapi.tiangolo.com/)
- **Pandas**: [10 Minutes to Pandas](https://pandas.pydata.org/docs/user_guide/10min.html)
- **Scikit-learn**: [User Guide](https://scikit-learn.org/stable/user_guide.html)

### 6.2. Vue.js & Frontend
- **Vue.js**: [Official Guide](https://vuejs.org/guide/introduction.html)
- **Pinia**: [Getting Started](https://pinia.vuejs.org/getting-started.html)
- **Vue Router**: [Guide](https://router.vuejs.org/guide/)
- **Chart.js**: [Documentation](https://www.chartjs.org/docs/latest/)
- **PrimeVue**: [Components](https://primevue.org/)

### 6.3. Docker & DevOps
- **Docker**: [Get Started](https://docs.docker.com/get-started/)
- **Docker Compose**: [Overview](https://docs.docker.com/compose/)
- **Git**: [Pro Git Book](https://git-scm.com/book/en/v2)

### 6.4. Machine Learning
- **Scikit-learn**: [Linear Models](https://scikit-learn.org/stable/modules/linear_model.html)
- **Cross-Validation**: [Scikit-learn CV](https://scikit-learn.org/stable/modules/cross_validation.html)

---

## 7. Практические упражнения для подготовки

### Упражнение 1: FastAPI + Pandas
```python
# Создать API эндпоинт для загрузки CSV и возврата статистики
@app.post("/api/stats")
async def get_stats(file: UploadFile):
    df = pd.read_csv(file.file)
    return {
        "shape": df.shape,
        "columns": df.columns.tolist(),
        "dtypes": df.dtypes.to_dict(),
        "stats": df.describe().to_dict()
    }
```

### Упражнение 2: Обучение модели
```python
# Реализовать обучение линейной регрессии с кросс-валидацией
def train_linear_model(X, y, model_type='ridge', **params):
    models = {
        'ridge': Ridge(**params),
        'lasso': Lasso(**params),
        'elastic': ElasticNet(**params)
    }
    model = models[model_type]
    
    # Кросс-валидация
    cv_scores = cross_val_score(model, X, y, cv=5, scoring='r2')
    
    # Обучение на всех данных
    model.fit(X, y)
    
    return model, {
        'cv_scores': cv_scores.tolist(),
        'mean_cv_score': np.mean(cv_scores),
        'coefficients': model.coef_.tolist()
    }
```

### Упражнение 3: Vue компонент
```vue
<script setup>
import { ref, computed } from 'vue'

const file = ref(null)
const uploadStatus = ref('idle')

const isFileSelected = computed(() => file.value !== null)

const handleFileChange = (event) => {
  file.value = event.target.files[0]
}

const uploadFile = async () => {
  if (!file.value) return
  
  uploadStatus.value = 'uploading'
  // Upload logic
  uploadStatus.value = 'success'
}
</script>

<template>
  <div class="upload-container">
    <input type="file" @change="handleFileChange" accept=".csv" />
    <button :disabled="!isFileSelected" @click="uploadFile">
      Загрузить
    </button>
    <p v-if="uploadStatus === 'uploading'">Загрузка...</p>
    <p v-else-if="uploadStatus === 'success'">Успешно!</p>
  </div>
</template>
```

---

## 8. Примерный план обучения

### Неделя 1-2: Backend Python
- Основы Python (3-4 дня)
- Pandas/NumPy (3-4 дня)
- Scikit-learn основы (3 дня)

### Неделя 3-4: FastAPI
- FastAPI основы (2 дня)
- Создание REST API (3 дня)
- Интеграция с ML (3 дня)

### Неделя 5-6: Frontend
- JavaScript/TypeScript (3 дня)
- Vue.js основы (4 дня)
- Компоненты и маршрутизация (3 дня)

### Неделя 7-8: Интеграция
- Axios и API (2 дня)
- State management (3 дня)
- Визуализация (3 дня)

### Неделя 9-10: DevOps
- Docker основы (3 дня)
- Docker Compose (2 дня)
- Деплой (3 дня)

---

## 9. Проверочный чек-лист

### Backend:
- [ ] Умею создавать FastAPI приложение
- [ ] Могу читать и обрабатывать CSV с pandas
- [ ] Знаю как обучить линейную модель в scikit-learn
- [ ] Понимаю как делать кросс-валидацию
- [ ] Могу создать красивые графики с matplotlib
- [ ] Знаю как реализовать эндпоинты для загрузки/экспорта

### Frontend:
- [ ] Понимаю Composition API во Vue 3
- [ ] Могу создавать реактивные компоненты
- [ ] Работаю с Pinia для управления состоянием
- [ ] Делаю HTTP запросы через Axios
- [ ] Строю графики с Chart.js/ECharts

### Docker:
- [ ] Пишу Dockerfile для Python приложения
- [ ] Настраиваю Docker Compose
- [ ] Управляю контейнерами и томами

### Общие:
- [ ] Работаю с Git (commit, push, pull, merge)
- [ ] Пишу логи и обработку ошибок
- [ ] Делаю простые тесты
- [ ] Понимаю структуру проекта

---
